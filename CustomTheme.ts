import { DefaultTheme } from 'react-native-paper';

const CustomTheme = {
    ...DefaultTheme,
    colors: {
      ...DefaultTheme.colors,
      primaryContainer: '#FFD874'
    }
  };

export default CustomTheme;